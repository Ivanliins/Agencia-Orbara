{
  "buildCommand": "pnpm install && pnpm --filter @workspace/orbara exec vite build --config vite.vercel.config.ts",
  "outputDirectory": "artifacts/orbara/dist",
  "installCommand": "pnpm install",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
